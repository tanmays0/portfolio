# Quickstart: Portfolio Site

```bash
cd /Users/shind/portfolio
npm install
npm run dev
```

Open http://localhost:3000 — redirects to `/en/about`.

```bash
npm run build
npx vercel --prod   # when ready
```

Switch locale via header FR/EN control. Theme via sun/moon toggle.
